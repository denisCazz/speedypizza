---
const currentYear = new Date().getFullYear();
---

<footer>
  <p>Speedy Pizza | Via Valobra 117/119, Carmagnola (TO)</p>
  <p class="delivery-info">🚚 Consegna a domicilio GRATUITA | Asporto disponibile</p>
  <p>&copy; {currentYear} Speedy Pizza. Tutti i diritti riservati.</p>
  <p>Sito realizzato da <a href="https://bitora.it" target="_blank" rel="noopener noreferrer">bitora.it</a></p>
</footer>

<style>
  footer {
    background: linear-gradient(135deg, var(--secondary-color) 0%, var(--secondary-dark) 100%);
    color: var(--light-gray);
    text-align: center;
    padding: 3rem 1rem 2rem;
    margin-top: auto;
    font-family: var(--font-primary);
    position: relative;
    overflow: hidden;
  }

  footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--primary-color), var(--primary-dark));
  }

  footer p {
    margin: 0.8rem 0;
    font-weight: 400;
    transition: color var(--transition-fast);
  }

  footer p:first-child {
    font-size: 1.1rem;
    font-weight: 500;
    color: white;
    margin-bottom: 1rem;
  }

  .delivery-info {
    font-size: 1rem;
    font-weight: 600;
    color: var(--primary-color);
    background: rgba(231, 76, 60, 0.1);
    padding: 0.8rem 1.5rem;
    border-radius: var(--border-radius);
    margin: 1.5rem auto;
    display: inline-block;
    border: 1px solid rgba(231, 76, 60, 0.3);
  }

  footer p:nth-child(4) {
    font-size: 0.9rem;
    color: var(--medium-gray);
    border-top: 1px solid rgba(236, 240, 241, 0.1);
    padding-top: 1.5rem;
    margin-top: 1.5rem;
  }

  footer p:last-child {
    font-size: 0.85rem;
    color: var(--medium-gray);
    margin-top: 0.5rem;
  }

  footer a {
    color: var(--primary-color);
    text-decoration: none;
    font-weight: 500;
    transition: color var(--transition-fast);
  }

  footer a:hover {
    color: white;
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    footer {
      padding: 2rem 1rem 1.5rem;
    }
    
    footer p:first-child {
      font-size: 1rem;
    }

    .delivery-info {
      font-size: 0.9rem;
      padding: 0.6rem 1rem;
    }
  }
</style>
